#include<stdio.h>
int main()
{
  int num,rev=0,rem;
printf("enter the no:");
scanf("%d",&num);
do
{
rem=num%10;
rev=rev*10+rem;
num=num/10;
}
  while(num!=0);
printf("reversed no=%d",rev);
return 0;
}
