#include<stdio.h>
int main()
{
  int n,m,r,rev=0;
printf("enter a number:");
scanf("%d",&n);
m=n;
do
{
r=n%10;
rev=rev*10+r;
n=n/10;
}
  while(n!=0);
if(rev==m)
  printf("%d is a palindrome number.",m);
else
  printf("%d is not a palindrome number.",m);
return 0;
}
