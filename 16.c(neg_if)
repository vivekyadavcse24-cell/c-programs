#include<stdio.h>
int main()
{
  int n;
printf("enter the no.:");
scanf("%d",&n);
if(n<0)
  printf("the no. is negative");
else if(n>0)
  printf("the no is positive");
else 
printf("the no. is 0");
return 0;
}
